Current state of each backend area:
AreaStateWhat's thereWhat's missingAuth✅ DoneLogin, signup, verify, /me, users CRUD, audit-logs, JWT identity fixedNothing for this passMessages/Inbox✅ DoneList + get + reply + AI toggle + read, all on canonical contractNothingWebhooks (routes.py)⚠️ PartialAll 5 channel routes exist, route into process_messageWebhooks don't currently link inbound messages to a Conversation row — _save_message creates User + Conv on the fly, but the inbox query won't find them unless conv tracking is solid. Will verify and patch.Pipeline (services.py)✅ DoneMulti-intent → Shopify context → AI gen → dispatch → saveNothing structural; depends on integrations (mocked) and AI (mocked)AI Generator✅ Stub works_mock_reply() builds a real reply from intents + contextKeep as-is (your call: stub)Shopify integration✅ Stub worksMock catalog + stockKeep as-is until credsMeta integration✅ Stub worksMock send for IG/WA/FBKeep as-is until credsTikTok integration✅ Stub worksMock sendKeep as-is until credsChannels❌ Missing(Only frontend mock exists)New blueprint: list channels, get config, update config, test connectionProducts❌ Missing(Cache table exists, no routes)New blueprint: list cached products, get one, manual sync (calls stubbed Shopify)AI Settings❌ Missing(Table exists, no routes)New blueprint: get/update single config rowAutomation Rules❌ Missing(Table exists, no routes)New blueprint: full CRUDLogs/Analytics❌ Missing(logs table exists, no routes)New blueprint: list with filters, aggregated stats endpoint for analytics pageDuplicate intent detector⚠️ Bug riskrouter.py exists with different labels + typoDelete or refactor